import React from 'react';
import './styles.css';
import Logo from '../logo';

export default () => {
  return <header className="header">
    <div className="text-center">
      <Logo />
    </div>
  </header>
}
